#include <stdio.h>

int main() {
    int numero;
    int resultado;

    printf("Escriba un numero: \n");
    scanf("%d", &numero);

    resultado = (numero ) * (numero ) - (25);

    printf("el cuadrado del numero es: %d", resultado);

    return 0;
}
